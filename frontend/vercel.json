{
  "git": {
    "deploymentEnabled": false
  },
  "buildCommand": "export SHELL=/bin/bash && pnpm run build",
  "installCommand": "export SHELL=/bin/bash && corepack enable && pnpm install",
  "framework": "nextjs"
}